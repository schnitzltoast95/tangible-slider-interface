<Window x:Class="TSI.ParticipantView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Participant View" Height="400" Width="600">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="GlobalStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Name="ContentPanel" Grid.Row="0" Margin="10">
            <TextBlock Name="TitleText" TextWrapping="Wrap" FontSize="20" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,10"/>

            <TextBlock Name="DescriptionText" TextWrapping="Wrap" FontSize="16" TextAlignment="Center" Margin="0,0,0,20"/>

            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Name="LeftLabel" FontSize="14" HorizontalAlignment="Left" TextAlignment="Right" Grid.Column="0" Margin="10,0,10,0" />
                <TextBlock Name="RightLabel" FontSize="14" HorizontalAlignment="Right" TextAlignment="Left" Grid.Column="1" Margin="10,0,10,0" />
            </Grid>
        </StackPanel>
    </Grid>
</Window>
